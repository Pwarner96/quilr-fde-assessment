# Quilr FDE Assessment

This repository will contain four independently runnable assessment components.
The foundation uses Python 3.12 and `uv`, with mocks and synthetic data as the
intended default. Run `make verify` for the root verification contract.

No Task 1–4 completion is claimed yet; this commit provides foundation only.
